import * as React from 'react';
import RouterView from '@/router';

const App: React.FC = () => {
  return (
    // <BrowserRouter>
    <RouterView />
    // </BrowserRouter>
  );
};

export default App;
