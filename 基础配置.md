# 项目配置
```text
├── 1. 项目目录规范
├── 2. 路径别名
├── 3. ESLint
├── 4. Prettier
├── 5. Husky + lint-staged
├── 6. less
├── 7. 环境变量
├── 8. axios封装
├── 9. React Router
├── 10. Zustand(Pinia替代)
├── 11. React Query
├── 12. UI组件库
├── 13. Icon方案
├── 14. 权限系统
├── 15. Layout
├── 16. 请求封装
├── 17. Mock
├── 18. 打包优化
├── 19. 自动导入
├── 20. Docker部署
```

## 1. 项目基础建立
- 初始化项目
- 清理官方模板
- 规划目录
    ```
    src
    │
    ├── api                 // 接口
    │
    ├── assets              // 静态资源
    │   ├── images
    │   ├── icons
    │   └── styles
    │
    ├── components          // 公共组件
    │
    ├── hooks               // 自定义Hook
    │
    ├── layout              // 布局
    │
    ├── views               // 页面
    │
    ├── router              // 路由
    │
    ├── store               // Zustand
    │
    ├── types               // TS类型
    │
    ├── utils               // 工具
    │
    ├── App.tsx
    │
    └── main.tsx
    ```
- styles目录
- 安装Less
- 配置路径别名
- 建立views
- utils目录
    ```
  utils

    request.ts
    
    storage.ts
    
    auth.ts
    
    download.ts
    
    date.ts
    
    validate.ts
  ```
  

---
## 2. 代码规范
加入 ESLint + Prettier + Husky + lint-staged
```text
写代码
   │
   ▼
ESLint（检查代码问题）
   │
   ▼
Prettier（统一代码格式）
   │
   ▼
git commit
   │
   ▼
lint-staged（只检查本次提交的文件）
   │
   ▼
Husky（Git 钩子，自动执行）
   │
   ▼
提交成功
```
- 安装ESLint

  React + Vite 现在其实已经有 ESLint
  - 安装 Prettier
  
    ```shell
    npm install -D prettier
    ```
    - 创建配置文件

      .prettierrc 就在项目根目录
    
      ```JSON
      {
      "printWidth": 100, // 一行最多100个字符。
      "tabWidth": 2, // 缩进2个空格。
      "semi": true, //自动加分号。
      "singleQuote": true, //统一单引号。
      "trailingComma": "all", // 最后一个也加逗号。
      "arrowParens": "always" //箭头函数统一规范。
      }
      ```
    - 创建 .prettierignore

      还是项目根目录
    
      ```text
      node_modules
      dist
      coverage
      ```
      这些目录不要格式化。
    - WebStorm 开启保存自动格式化

      Settings -> Languages & Frameworks -> JavaScript -> Prettier



      
- 让 ESLint 和 Prettier 配合

  - 安装 eslint-config-prettier
  
    ```shell
    npm add -D eslint-config-prettier
    ```
  - 修改 eslint.config.js
  
    ```js
    import eslintConfigPrettier from 'eslint-config-prettier' // 导入
    ```
    原本：
    ```js
    extends: [
    js.configs.recommended,
    ...tseslint.configs.recommended,
    ],
    ```
    改为：
    ```js
      extends: [
      js.configs.recommended,
      ...tseslint.configs.recommended,
      eslintConfigPrettier,
      ],
      ```


## 3. git提交规范
  
## 4. React Router
- 安装react-router

  ```shell
  react-router-dom@6
  ```

## 5. Layout 页面布局
- Header / Sidebar / TagsView / Main拆分
- Ant Design Layout
- Redux管理Layout状态
- Sidebar折叠
- Sider响应式
- 嵌套路由基础配置
- 只展开一个父菜单（未完成）
- 动态路由和Menu （未完成）
  1. 抽离动态路由初始化
  2. permission store 增加 routes
  3. 实现路由守卫
  4. 做登录
  ```json
  ✅ 后端动态路由返回
  
  ✅ transformRoutes
  
  ✅ 动态加载组件
  
  ✅ permission store
  
  ✅ 动态菜单
  
  
  下一步：
  
  1. auth登录状态(store)
  2. token管理
  3. 路由守卫
  4. / 和 /login 跳转
  5. 刷新页面恢复权限
  6. 按钮权限
  7. 标签页缓存
  ```
  
- 主题切换
- Layout视觉优化
- 统一icon
- svg
  
  svg 跟随系统颜色
  ```
     将fill 改为currentColor 
  ```

## 6. 基础业务开发准备阶段
- 完善请求封装
- 封装Storage
- 登录流程
- 路由权限拦截
- 面包屑 Breadcrumb
- 页面组件规范
- 最后再做动态菜单

     